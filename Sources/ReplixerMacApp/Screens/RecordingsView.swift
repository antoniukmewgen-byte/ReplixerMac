import SwiftUI

/// Phase 7.2 placeholder. Windows parity source: `RecordingsViewModel`
/// (170 lines — simple CRUD+list view over `RecordingHistory`/
/// recordings.json). Real content lands in Phase 7.4.
struct RecordingsView: View {
    var body: some View {
        PlaceholderScreen(title: "Записи", systemImage: "waveform")
    }
}

#Preview {
    RecordingsView()
}
