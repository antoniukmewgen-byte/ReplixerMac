import SwiftUI

/// Phase 7.2 placeholder. Windows parity source: `SettingsViewModel` (83
/// lines — the simplest screen, a thin proxy over `AppSettings`). Real
/// content lands in Phase 7.3.
struct SettingsView: View {
    var body: some View {
        PlaceholderScreen(title: "Налаштування", systemImage: "gearshape")
    }
}

#Preview {
    SettingsView()
}
