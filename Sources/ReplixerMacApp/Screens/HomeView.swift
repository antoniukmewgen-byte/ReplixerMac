import SwiftUI

/// Phase 7.2 placeholder. Windows parity source: `HomeViewModel` (699
/// lines, the most complex screen — call-detection status, recent
/// recordings list, report form, upload orchestration). Real content lands
/// in Phase 7.5.
struct HomeView: View {
    var body: some View {
        PlaceholderScreen(title: "Головна", systemImage: "house")
    }
}

#Preview {
    HomeView()
}
